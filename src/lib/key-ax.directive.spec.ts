import { KeyAxDirective } from './key-ax.directive';

describe('KeyAxDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyAxDirective();
    expect(directive).toBeTruthy();
  });
});
